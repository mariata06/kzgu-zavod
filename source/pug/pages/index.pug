extends ../components/base/layout

block variables
  - const title = "Казанский завод генераторных установок" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    //- +intro()
    +mainscreen()
    +users()
    +bestsellers()
    +consult(false, false, {formTitle: 'Нужна консультация?', formNote: 'Оставьте заявку и наш специалист<br>перезвонит вам в ближайшее время'})
    +specialist()
    +info()
    +last-news()
    +projects(projectsHome)
    +seo()

block modals
